# analysis.py
# Import the script and execute functions or code if necessary

# Importing the script as a module (if it contains callable functions)
# import script

# If the script is standalone, you can execute it directly like this:
if __name__ == "__main__":
    # Execute script file
    exec(open("/mnt/data/script.py").read())
